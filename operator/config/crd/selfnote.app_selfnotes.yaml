---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: selfnotes.selfnote.app
spec:
  group: selfnote.app
  names:
    kind: Selfnote
    listKind: SelfnoteList
    plural: selfnotes
    shortNames:
    - sn
    singular: selfnote
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.host
      name: Host
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Selfnote is a self-hosted collaborative workspace instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SelfnoteSpec is the desired state of a Selfnote instance.
            properties:
              api:
                description: ComponentSpec configures one of the Selfnote services.
                properties:
                  image:
                    description: Image name (without registry); the registry comes
                      from spec.imageRegistry.
                    type: string
                  replicas:
                    default: 2
                    format: int32
                    type: integer
                type: object
              appSecret:
                description: Name of the Secret with JWT_SECRET / ROOM_SECRET.
                type: string
              host:
                description: Public hostname for the Ingress.
                type: string
              imageRegistry:
                default: ghcr.io/fulviodenza
                description: Container image registry, e.g. ghcr.io/fulviodenza.
                type: string
              postgres:
                description: PostgresSpec configures the managed Postgres cluster.
                properties:
                  backup:
                    description: BackupSpec configures CloudNativePG continuous +
                      scheduled backups.
                    properties:
                      credentialsSecret:
                        description: Name of the Secret holding ACCESS_KEY_ID / ACCESS_SECRET_KEY.
                        type: string
                      destinationPath:
                        description: e.g. s3://selfnote-backups/pg
                        type: string
                      enabled:
                        default: true
                        type: boolean
                      endpointURL:
                        description: S3-compatible endpoint (MinIO), e.g. http://<release>-minio:9000
                        type: string
                      schedule:
                        default: 0 0 2 * * *
                        type: string
                    type: object
                  instances:
                    default: 3
                    format: int32
                    type: integer
                  storageSize:
                    default: 10Gi
                    type: string
                type: object
              sync:
                description: ComponentSpec configures one of the Selfnote services.
                properties:
                  image:
                    description: Image name (without registry); the registry comes
                      from spec.imageRegistry.
                    type: string
                  replicas:
                    default: 2
                    format: int32
                    type: integer
                type: object
              version:
                default: 0.1.0
                description: Application version / image tag.
                type: string
              web:
                description: ComponentSpec configures one of the Selfnote services.
                properties:
                  image:
                    description: Image name (without registry); the registry comes
                      from spec.imageRegistry.
                    type: string
                  replicas:
                    default: 2
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: SelfnoteStatus is the observed state of a Selfnote instance.
            properties:
              conditions:
                description: 'Conditions: Ready, DBReady, BackupHealthy.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
